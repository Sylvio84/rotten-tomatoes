<form method="post" action="{{ path('movie_delete', {'slug': movie.slug}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer ce film?');" style="display: inline-block">
    <input name="_method" type="hidden" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ movie.id) }}">
    <button class="btn btn-danger">
        <i class="fas fa-trash"></i>
        Supprimer</button>

</form>
